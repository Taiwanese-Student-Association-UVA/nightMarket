import BackButton from "../BackButton.tsx";

export default function Schedule() {
    return (
        <div style={{ padding: 20 }}>
            <BackButton />
            <h1>Schedule</h1>
        </div>
    );
}